<%- include("partials/header.ejs") %> 
    

    <h1>Hellooo welcome</h1>

    <p><%= message %> </p>

    <% blogList.forEach(function(blog) { %>
        <h2><%= blog.title %></h2>
        <p><%= blog.content.substring(0,200) %>..... <a href="/blog/<%= blog._id %> ">Read More</a></p>
    <% });%>

<%- include("partials/footer") %> 
